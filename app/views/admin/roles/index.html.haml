%h1 Listing roles

%table
  %thead
    %tr
      %th User
      %th Title
      %th Status
      %th Salary
      %th Salary terms
      %th
      %th
      %th

  %tbody
    - @roles.each do |role|
      %tr
        %td= role.user
        %td= role.title
        %td= role.status
        %td= role.salary
        %td= role.salary_terms
        %td= link_to 'Show', role
        %td= link_to 'Edit', edit_role_path(role)
        %td= link_to 'Destroy', role, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Role', new_admin_role_path
